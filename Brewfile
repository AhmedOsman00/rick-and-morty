# Brewfile for Arrow Generator development dependencies
#
# Install all dependencies with:
#   brew bundle
#
# Update all dependencies with:
#   brew bundle --force
#
# Check for missing dependencies:
#   brew bundle check

# Required dependencies
# =====================

# Swift tool version manager - manages SwiftLint and other Swift tools
brew "mint"
